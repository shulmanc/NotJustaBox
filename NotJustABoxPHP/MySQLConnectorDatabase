import mysql.connector

db= mysql.connector.connect(
	host = "localhost",
	user = "root",
	passwd = "password123",
	database = "NotJustaBox"
	)

mycursor=db.cursor()

#mycursor.execute("CREATE TABLE newusers (name VARCHAR(255), email VARCHAR(255), store_name VARCHAR(255), store_address VARCHAR(255), quantity INTEGER(100), user_id INTEGER AUTO_INCREMENT PRIMARY KEY)")

mycursor.execute("SHOW TABLES")
for table in mycursor:
	print (table[0])